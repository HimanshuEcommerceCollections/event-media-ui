node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
.env*.local
next-env.d.ts

# Regenerated by `next dev` on every run; kept local, not tracked.
AGENTS.md
CLAUDE.md

# Static design references. Kept local: ~77 MB, and the media inside them is
# already extracted to public/assets.
designs/
