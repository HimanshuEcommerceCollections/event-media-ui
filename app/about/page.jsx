import AboutView from "./AboutView";

export const metadata = {
  title: "About — Events & Media",
  robots: { index: false, follow: false },
};

export default function AboutPage() {
  return <AboutView />;
}
