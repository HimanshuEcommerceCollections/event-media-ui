import AdminGalleryView from "./AdminGalleryView";

export const metadata = {
  title: "Gallery — Admin — Events & Media",
  robots: { index: false, follow: false },
};

export default function AdminGalleryPage() {
  return <AdminGalleryView />;
}
