import AdminLegalView from "./AdminLegalView";

export const metadata = {
  title: "Legal — Admin — Events & Media",
  robots: { index: false, follow: false },
};

export default function AdminLegalPage() {
  return <AdminLegalView />;
}
