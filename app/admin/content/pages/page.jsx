import AdminContentPagesView from "./AdminContentPagesView";

export const metadata = {
  title: "Pages — Admin — Events & Media",
  robots: { index: false, follow: false },
};

export default function AdminContentPagesPage() {
  return <AdminContentPagesView />;
}
