import AdminReviewsView from "./AdminReviewsView";

export const metadata = {
  title: "Reviews — Admin — Events & Media",
  robots: { index: false, follow: false },
};

export default function AdminReviewsPage() {
  return <AdminReviewsView />;
}
