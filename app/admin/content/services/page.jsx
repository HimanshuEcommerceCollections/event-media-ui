import AdminServicesView from "./AdminServicesView";

export const metadata = {
  title: "Services — Admin — Events & Media",
  robots: { index: false, follow: false },
};

export default function AdminServicesPage() {
  return <AdminServicesView />;
}
