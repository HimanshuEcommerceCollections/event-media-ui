import AdminOverviewView from "./AdminOverviewView";

export const metadata = {
  title: "Admin — Events & Media",
  robots: { index: false, follow: false },
};

export default function AdminOverviewPage() {
  return <AdminOverviewView />;
}
