import AdminUsersView from "./AdminUsersView";

export const metadata = {
  title: "Users — Admin — Events & Media",
  robots: { index: false, follow: false },
};

export default function AdminUsersPage() {
  return <AdminUsersView />;
}
