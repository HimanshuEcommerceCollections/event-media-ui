import AdminVendorAccountsView from "./AdminVendorAccountsView";

export const metadata = {
  title: "Vendor accounts — Admin — Events & Media",
  robots: { index: false, follow: false },
};

export default function AdminVendorAccountsPage() {
  return <AdminVendorAccountsView />;
}
