import BuildView from "./BuildView";

export const metadata = {
  title: "Build my event — Events & Media",
  robots: { index: false, follow: false },
};

export default function BuildPage() {
  return <BuildView />;
}
