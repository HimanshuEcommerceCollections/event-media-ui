import CommercialView from "./CommercialView";

export const metadata = {
  title: "Commercial — Events & Media",
  robots: { index: false, follow: false },
};

export default function CommercialPage() {
  return <CommercialView />;
}
