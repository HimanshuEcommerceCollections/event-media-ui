import ContactView from "./ContactView";

export const metadata = {
  title: "Contact — Events & Media",
  robots: { index: false, follow: false },
};

export default function ContactPage() {
  return <ContactView />;
}
