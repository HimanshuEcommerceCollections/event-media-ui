import DashboardView from "./DashboardView";

export const metadata = {
  title: "Dashboard — Events & Media",
  robots: { index: false, follow: false },
};

export default function DashboardPage() {
  return <DashboardView />;
}
