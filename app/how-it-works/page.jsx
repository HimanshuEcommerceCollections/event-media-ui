import HowItWorksView from "./HowItWorksView";

export const metadata = {
  title: "How it works — Events & Media",
  robots: { index: false, follow: false },
};

export default function HowItWorksPage() {
  return <HowItWorksView />;
}
