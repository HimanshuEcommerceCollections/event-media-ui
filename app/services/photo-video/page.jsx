import PhotoVideoView from "./PhotoVideoView";

export const metadata = {
  title: "Photo + video — Events & Media",
  robots: { index: false, follow: false },
};

export default function PhotoVideoPage() {
  return <PhotoVideoView />;
}
