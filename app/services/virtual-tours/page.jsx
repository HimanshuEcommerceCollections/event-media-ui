import VirtualToursView from "./VirtualToursView";

export const metadata = {
  title: "Virtual tours — Events & Media",
  robots: { index: false, follow: false },
};

export default function VirtualToursPage() {
  return <VirtualToursView />;
}
