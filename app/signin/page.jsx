import SignInView from "./SignInView";

export const metadata = {
  title: "Sign in — Events & Media",
  robots: { index: false, follow: false },
};

export default function SignInPage() {
  return <SignInView />;
}
