import VendorJobsView from "./VendorJobsView";

export const metadata = {
  title: "Jobs — Vendor — Events & Media",
  robots: { index: false, follow: false },
};

export default function VendorJobsPage() {
  return <VendorJobsView />;
}
