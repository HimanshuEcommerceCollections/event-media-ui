import VendorProfileView from "./VendorProfileView";

export const metadata = {
  title: "Profile — Vendor — Events & Media",
  robots: { index: false, follow: false },
};

export default function VendorProfilePage() {
  return <VendorProfileView />;
}
