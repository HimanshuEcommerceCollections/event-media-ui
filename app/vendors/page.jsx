import VendorsView from "./VendorsView";

export const metadata = {
  title: "Become a vendor — Events & Media",
  robots: { index: false, follow: false },
};

export default function VendorsPage() {
  return <VendorsView />;
}
